using System;
using Dynamo.Core;
using Dynamo.Extensions;
using Dynamo.Graph.Nodes;

namespace FindMyNode
{
    class FindMyNodeViewModel : NotificationObject, IDisposable
    {
        public void Dispose()
        {
            throw new NotImplementedException();
        }
    }
}
